[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "catch-all-email-verifier"
version = "1.0.0"
description = "Verify emails on catch-all domains with deterministic verification. Powered by Enrow."
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
authors = [{ name = "Enrow" }]
keywords = [
    "catch-all",
    "catch-all-verifier",
    "catch-all-email",
    "email-verifier",
    "email-verification",
    "email-validation",
    "deliverability",
    "deterministic",
    "b2b",
    "enrow",
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = ["httpx>=0.24"]

[project.urls]
Homepage = "https://enrow.io"
Documentation = "https://docs.enrow.io"
Repository = "https://github.com/EnrowAPI/catch-all-email-verifier-python"
